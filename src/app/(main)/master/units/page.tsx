import React from "react";
import { Metadata } from "next";
import MasterUnitsList from "@/app/components/master/units/list";

export const metadata: Metadata = {
  title: "Units List",
};

// Use an implicit return for conciseness
const Master๊nitsListPage = () => <MasterUnitsList />;

export default Master๊nitsListPage;
